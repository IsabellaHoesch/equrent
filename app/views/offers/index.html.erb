<div class= "container">  

  <div>
    <h1>Equipment close to you!</h1>
    <p>Rent outdoor equipment from people near you! 
      <br> We offer equipments ranging from P like Ping-Pong rackets to Z like Zorb Football - a team sport in which players are encased in an inflated bubble called Zorb.
      <br> Rent out your equipment to sport-enthusiasts near you and make some cash with Equrent today!</p>
  </div>

  <%= link_to "Create an offer", new_offer_path, class: "button btn btn-outline-secondary mb-3" %>
  <div class="main-container">
    <div class="cards">
      <% @offers.each do |offer| %>
        <div class="card">
          <img src="<%= offer.offer_img %>">
          <div class="card-product-infos">
            <h3><%= link_to offer.name, offer_path(offer) %></h3>
            <p><%= offer.sport_type %></p>
            <p><%= offer.description %></p>
          </div>
        </div>
      <% end %>
    </div>

    <div id="map"
        style="width: 100%; height: 600px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
</div>


