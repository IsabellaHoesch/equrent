

  <%= video_tag "cover_video", autoplay: :autoplay, loop: :loop, muted: :muted, class: 'banner'%>
 
   <div class="container">
    <div id="banner-content">
      <h1>
        <span id="banner-typed-text"></span>
      </h1>
       <%= link_to "Check sport equipment nearby", offers_path, class: 'btn btn-primary' %>
    </div>  
    
    <div class="w-75" id="about-us">
      <h2>Tired of doing the same activity every weekend?</h2>
      <p>
      Equrent is finally bringing the shared economy to your sports -and activity life. 
      You don´t have a massive cellar to store your equipment? You want to try new hobbies but don´t want to buy or rent expensive gear? 
      <br>
      No problem! 
      With Equrent you can find any equipment and gear close-by. 
      <br>
      While you´re using other peoples equipment, why not make some extra bucks and rent out your equipment too?
      </p>
    </div>

    <div id="home-buttons">
      <%= link_to "Show all equipment", offers_path, class: 'btn btn-primary' %>
      <%= link_to "Place your offer", new_offer_path, class: 'btn btn-primary' %>
    </div>

    <h2>Latest offers</h2>
  
  <div class="main-container">
    <div class="cards">
      <% @offers[0..5].each do |offer| %>
        <div class="card <%= "d-none" if offer.bookings.present? %>">
          <% if offer.photo.attached? %>
          <%= cl_image_tag offer.photo.key, height: 300, width: 400, crop: :fill %>
          <% else %>
          <%= image_tag 'sports_img.jpg', height: 300, width: 400, crop: :fill %>
          <% end %>
          <div class="card-product-infos">
            <h3 id="btn-title"><%= link_to offer.name, offer_path(offer) %></h3>
           <div id="card-content">
              
                <p><%= offer.sport_type %></p>
                <p><%= offer.description %></p>
                <h3><%= offer.price %> €</h3>
            </div>
          </div>
        </div>
      <% end %>      
    </div>


  </div>

